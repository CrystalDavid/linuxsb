# Codex Instructions — 烧饼社区 M3 原生社区壳与交互闭环

## 当前唯一任务

在不推翻 P0、P0-B 和 M1 已验证架构的前提下，完成简约、蓝色、纯 ArkUI 的社区主流程：

```text
底部：首页 / 板块 / 称号 / 我的；首页右上角搜索、首页独立发布 FAB
  ↓
真实列表、真实头像、连续分页
  ↓
原生主题详情、楼中楼、图片和简约回复栏
```

当前分支为 `codex/m3-native-community-shell`。下方 M2 的阶段结论已转入 `docs/project-status.md` 作为历史；如与本文冲突，以本 M3 指令为准。

## 必须保持的架构

- 正常同源业务：`ArkWeb 同源 fetch -> 版本化轻量解码 -> TaskPool -> 领域模型 -> PresentationMapper -> ArkUI`；ArkWeb 尚未就绪时允许 RCP 作兼容回退，第三方匿名图床仍使用 RCP。
- Page / Component 不直接解析 HTML、读 Cookie 或绕过 Repository 请求网络。
- 不构建 DOM，不使用 CSS 选择器，不使用整页正则解码。
- ArkWeb 只允许在 `services/auth` 中由 `ArkWebTransportHost` 和 `OfficialLoginPage` 创建。正常页面保留 1 个全屏、不可命中、被原生根壳完全遮挡的同源传输宿主；只有官方登录或 Cloudflare 挑战需要用户操作时才允许显示网页。可见业务 UI 不得改成网页容器。
- Cookie 只保留 `bbs_auth` / `bbs_csrf` 所需的内存 Header，不输出值，不写 Preferences 或文件。
- 全局复用正式 `RcpForumTransport` / Session，主题页保留缓存、在途去重和立即导航。

## UI 基线

- 根导航和页签使用 `HdsNavigation / HdsNavDestination / HdsTabs`；可见底栏只承载四个 Tab：`首页 / 板块 / 称号 / 我的`，搜索位于首页右上角。四栏托盘固定为 264×56vp。首页在右侧以 8vp 间隔放置同高的 56×56vp 发帖加号，总宽 328vp；“我的”在同一位置显示系统设置按钮；这两枚右侧动作必须使用 `SemanticColors.textPrimary()`，即浅色黑、深色白，禁止恢复品牌蓝。板块与称号不显示右侧动作，四栏托盘通过既有弹簧曲线向右移动 32vp 后居中。托盘只允许一个玻璃采样层和一个 60×50vp 连续移动的选中指示器，按钮自身禁用默认 pressed/hover 状态层，避免黑闪和双层轮廓。
- 四个底栏图标只保留一套鸿蒙原生实现。板块、称号和我的分别使用系统填充 `folder_fill`、`star_rosette_fill`、`person_fill`；首页为兼容 API 23–26 的一致轮廓，使用 ArkUI `Path` 绘制。`ImmersiveBottomTabBar.ets` 的当前视觉实现已冻结，未经用户明确要求不得修改；交付前必须通过 `scripts/CheckBottomTabVisualLock.ps1`。首页右侧发帖按钮使用 27vp 系统 `plus`、900 字重；“我的”右侧设置按钮使用 27vp `gearshape_fill`。
- 玻璃“通透”风格必须以透亮、低遮罩、保留底层色彩为目标。HDS 标题栏使用设备能力探测后的 `ADAPTIVE` 材质类型/档位，不得固定 `EXQUISITE`。当前 target API 24 产品的普通组件统一使用唯一一层 `backgroundEffect`；通透固定为 4vp / 1.42 / 0.98 / 低 alpha 冷色 tint，深浅色分别取资源。API 26 的通用 `ImmersiveMaterial` 是后续 target 26 产品能力，不能把它误写成当前 API 24 已执行路径，也不能在迁移时删除 API 23–25 回退。
- 语义主色为蓝色；禁止绿色品牌色泄漏。
- 页面背景保持单一底色；`surface / surface_secondary / surface_elevated / settings_group_bg` 在深浅色中都必须与 `page_background` 同色。列表卡片、称号卡片、空态与表单分组只靠间距、细边和圆角建立层级，禁止恢复纯白或另一块深灰结构底板。唯一结构卡片例外是设置总览的两组系统样式分组：只允许这些节点使用 `settings_overview_card_bg`（浅色白、深色深灰）；不得把此资源扩散到其他页面。仅输入、分段切换和悬浮导航等交互层允许使用半透明液态玻璃。
- 标题栏在当前 HDS 能力范围内使用渐进模糊；API 26 原生 `ScrollEffectOptionsBeta/systemMaterial` 只有迁移到 API 26 SDK/target 后才能启用。当前 API 23–24 通透路径只能保留唯一一层 `backgroundEffect`，使用小半径、低 alpha tint 和增强饱和度保留底层色块，不能恢复 60vp 大模糊或厚白遮罩。深浅色分别读取资源。同一节点禁止第二个模糊层或额外不透明底板。
- 搜索和个人页的模式切换继续使用 `CompactSegmentedControl`；首页与具体板块页使用标题栏右端筛选文字按钮（40vp 高、15vp/Medium 居中文字、无 `chevron_down`）与六项玻璃下拉菜单；首页搜索为 40×40vp、24vp 图标。搜索图标和筛选文字必须使用 `SemanticColors.textPrimary()`，即浅色黑、深色白。浅色模式的活动指示器必须使用可辨认的中性灰胶囊，不得与页面背景融为一体。分段组件必须支持水平拖动并复用共享通透配方：4vp / 1.42 / 0.98 / 低 alpha 冷色 tint，边缘 0.8vp、10vp 单圈阴影和移动胶囊参数。四个根页面左上角标题统一使用 28vp、Bold；压栈子页标题保持 22vp、Bold。
- 称号中心顶部六个横向滚动胶囊，以及交易页“在售交易 / 发布交易 / 我的交易”胶囊，必须复用共享单层通透配方、0.8vp 高光边与单圈阴影，关闭系统默认 pressed/hover 蒙层；未选中背景透明，选中项以主题主前景实底和页面色文字保持明确状态，不得重新使用纯白底。
- 业务文字只允许 `Typography` 的 13 档固定字号：11/12/13/14/15/16/17/18/20/22/24/26/28vp；系统 Symbol、Path 和图标尺寸不计入文字字号。根页面标题 28vp/Bold，压栈页面标题 22vp/Bold，主题与板块名称 17vp/500，紧凑主题 16vp/400，正文 15vp，元信息 13vp，说明/分段标签 12vp，底栏标签 11vp。不得在业务 `Text / Button / TextInput / TextArea` 中新增裸数字字号。
- 板块根页只显示真实板块目录，并使用 16vp 不同色标、板块名、官方简介和真实主题数；目录行右侧不得显示进入箭头，整行仍可点击进入。板块目录名称和进入具体板块后的主题标题都与首页统一为 17vp、500 字重，目录主题数与其他元信息统一为 13vp。根目录首行必须紧邻标题栏，不得恢复大块顶部空白。首页仅向用户提供并持久化两种展示：默认“标题优先”为标题、标签、作者/回复/时间三行，头像 48vp；“作者优先”为头像旁作者与真实最近活跃时间/回复数、下方标题、末行标签，头像缩至 38vp。两种首页标题均为 17vp、500 字重，不得重新暴露旧样式一；非首页主题流继续使用原紧凑结构，仅板块流增强标题字形。“我的”主题/回帖流是唯一额外尺寸覆盖：头像 40vp，标题到分类标签/作者行间距 8vp；不得把这一覆盖扩散到首页或板块流。首页与进入具体板块后的标题栏不再使用“新评论 / 新帖子”分段切换器：右端改为单枚 40vp 高玻璃筛选按钮，只显示居中的 15vp 当前排序文字（新评论 / 新帖子 / 精华 / 抽奖 / 发卡 / 足迹），不显示展开箭头；点击展开六项玻璃下拉菜单，选中后按钮文字与列表同帧切换；板块目录页不显示筛选按钮，板块名右侧与首页同款但无搜索。帖子状态统一使用浅色语义胶囊：置顶蓝、精华紫、热红、抽奖中粉、进行中卡片青、已结束卡片灰，并为深色模式提供对应高对比色。
- 搜索提交、官方登录和主题回复发送等高频操作不得使用纯色填充按钮；统一复用 4vp 单层通透配方、细高光边框和轻光晕，图标或文字使用品牌蓝。搜索输入移除 `TextInput` 默认左右内边距，图标与文字间距为 6vp；搜索提交按钮与输入外层同为 42vp，必须复用回复按钮同一个 24vp、800 字重系统 `arrow_up`，只以 90° 旋转形成向右箭头。未登录搜索页不得构建输入、提交或范围切换控件，只显示与“我的”完全同尺寸、同位置的共享登录门禁，两页父级顶部内边距统一为 24vp。主题回复发送按钮与输入框外层均为 42vp 高；按钮内使用 24vp、800 字重的系统 `arrow_up`，无论当前是否登录或草稿是否为空，视觉上都保持品牌蓝且不降低透明度；只画箭头，不得叠加 `arrow_up_circle_fill` 或第二个蓝色实心圆底，未登录点击仍进入官方登录。
- 搜索范围“标题 / 正文 / 回帖”不得跟随主搜索框放大；固定为每段 52vp 宽、32vp 高的紧凑满圆胶囊，保持水平拖动与清晰选中态。
- 主页不永久显示“非官方客户端”；身份说明保留在登录/关于等合适位置。
- 列表使用 linux.sb 真实头像；只在缺失或加载失败时显示首字母回退。
- 主楼、回复和楼中楼均为头像居左、作者/正文居右；使用克制的局部分隔，不还原网页容器和强分割线。详情页滚动折叠标题栏中的分类标签必须相对标题起点右缩 12vp，不得贴回返回按钮一侧。
- 图片使用原生 `Image` 可视区懒加载，不阻塞文字首帧；失败时可重试且不让页面停留 Loading。
- 主题详情与发表主题页共用同一返回按钮：40vp 单层玻璃点击区，`chevron_left` 使用 24vp、600 字重，并保持相同标题栏内边距。回复输入不保留左侧编辑图标，只使用输入文字和单一发送按钮。帖子内“回复”动作必须使用胶囊圆角并裁剪点击状态，禁止出现矩形闪层。
- 发表主题页未登录时不得渲染发布按钮、板块选择、标题或正文输入，只显示登录门禁；门禁使用顶部锚定的独立布局，固定在“发表主题”标题栏正下方，取消边框、背景框和大块垂直留白。登录后才显示完整编辑器：标题输入（48vp）与正文输入在上，板块选择器在下。真实板块必须全部以三列 40vp 满圆 Chip 直接展示；未选中 Chip 使用共享单层玻璃与 0.85vp 高光边，选中 Chip 使用 `compose_forum_selected_bg` 品牌蓝底与品牌蓝边，不得使用灰色选中态。发布操作使用 40vp 圆润单层玻璃胶囊。
- 官方登录覆盖页的原生头部必须读取 `SYS_TOP_INSET_KEY`，标题与“完成并返回”按钮完整避开状态栏和挖孔区；不显示“临时网页登录……”说明。登录正文仍是唯一允许的临时 ArkWeb，普通页面不得出现 Web 节点。
- 用户已确认获得 ArkDO 作者授权；可在授权范围内参考、移植和改造固定上游提交 `7680996437b3b877aa5c69ac2f55529297a2ea52`，但保留烧饼社区自己的品牌、协议模型和网络架构。

## 数据与交互契约

- 首页根据服务端真实 `p` 链接连续加载，不得再有 30 条客户端上限；合并时按 topicId 去重。
- 板块页只展示服务端实际板块，选中后通过真实 `/forum/{id}` GET 筛选。
- 搜索使用真实 GET 契约，尊重服务端登录门控，不伪造结果。
- “我的”使用原生页面展示当前用户、主题和回复，不使用 ArkWeb 作业务容器；外观设置只放在独立原生设置页，由“我的”底栏右侧 56×56vp 系统齿轮按钮进入，不在列表上叠加可拖动 FAB。
- 设置页必须保持“总览列表 → 可视化选择详情”的两级结构。总览删除“显示与布局”分区标题，使用两张 18vp 圆角系统样式卡片：第一组展示“外观主题 / 首页帖子样式”，第二组展示“投喂开发者 / 反馈”；浅色卡片为白色，深色使用专用深色卡面。每行固定高 56vp，中间使用左右各 18vp 缩进的 0.5vp `outline` 分割线；左侧标题 17vp/500，当前值 16vp，右箭头 20vp，并保持 12vp 右内边距与垂直居中。主题详情只以两列大预览展示浅色与深色，预览内部底部的四个状态色圆点必须完整位于裁剪边界内，并在预览下方单独提供“跟随系统”开关。帖子样式详情用真实信息顺序预览标题优先和作者优先。不得恢复“点击设置项查看效果预览”等介绍段落。选择状态必须先更新页面本地响应式状态，再由管理器应用并持久化，保证蓝色边框与圆形勾选在点击当帧移动；三页短内容都固定在标题栏下方，不得重新垂直居中。
- “投喂开发者”使用 ArkUI 底部弹窗，四行固定为“投喂一个硬币 ¥1.00 / 投喂一瓶水 ¥5.00 / 投喂一杯咖啡 ¥9.90 / 投喂一个 pro 订阅 ¥50.00”。四档均为华为 IAP 一次性消耗型商品，不得误写成自动续费订阅；点击档位才允许调用 IAP Kit 打开华为收银台。商品未在 AGC 配置、用户取消或环境不可用时必须显示明确结果，不得伪造支付成功。反馈行固定进入原生 `topic/17803` 详情，不得改成外部浏览器或 ArkWeb。
- 新主题只允许 POST `/topic_edit`，回复只允许 POST `/reply_edit`；字段必须匹配 BBS1 v8.6.5 真实表单契约。
- 写请求必须同时拥有内存登录会话和 CSRF，且只能由用户在编辑页明确点击。自动测试只使用 fake transport，不得生成真实社区内容。
- 华为 IAP 与论坛会话完全隔离：不得向 IAP Kit 传递 Cookie、CSRF、私信、正文或账号密码；不得在日志和仓库中保存购买数据、签名或支付凭据。邀请测试前必须在 AGC 创建四个固定商品 ID，并用沙盒测试账号分别验证取消、失败、成功与消耗确认。
- P0 报告中 `P0-8` 的历史结论继续为 `NOT RUN`；M3 的真实发帖/回复远程验收也保持 `NOT RUN`，直到用户明确提供测试内容并确认发送。

## API 23–26 兼容契约（不得删除）

- 当前活动产品必须保持 `targetSdkVersion: 6.1.1(24)` 与 `compatibleSdkVersion: 6.1.0(23)`；同一 HAP 需能安装到 API 23/24 与更高系统。没有 API 26 SDK 构建证据时，禁止把 target 或普通组件材质写成 API 26 已启用。
- API 23–24 普通组件只允许一层 `backgroundEffect`；全局只保留通透配方 4vp / 1.42 / 0.98 / 低 alpha 冷色 tint。已删除奶白配方与设置入口，升级时必须把旧持久值强制迁移为 `TRANSLUCENT`。深色和浅色必须读取各自资源，不得硬编码白色 tint。
- HDS 分支：`HdsNavigation / HdsNavDestination / HdsTabs` 保持 `getSystemMaterialTypes()` 能力探测，支持时优先 `ADAPTIVE`，不支持时不得强行启用或叠第二层白色背板。
- API 26 的通用 `ArkUI_ImmersiveMaterial`、普通组件 `systemMaterial` 与标题栏 `ScrollEffectOptionsBeta` 是 26.0.0 起始能力。以后建立 target 26 产品时必须用 API 可用性守卫，并继续保留上述 API 23–24 回退；迁移不是删除兼容层的理由。
- 验收拆成两台 Pura 90：API 24 验证安装、启动、深浅色和回退玻璃；API 26 验证同一 HAP 的向上运行兼容与 HDS 能力探测。只有另有 target 26 构建时，才能额外宣称普通组件原生 `ImmersiveMaterial` 命中。API 26 模拟器也不能替代 API 26 真机性能结论。

## 安全与 Git

- 可共享的 `build-profile.json5` 不得包含本机签名密码、证书路径、keystore 路径或 Profile 路径；本地调试签名由 DevEco Studio 在开发机上单独配置，不能提交到 Git。
- 不提交 `artifacts/`、日志、截图、HTML 快照、Cookie 或签名材料。
- 不修改 `docs/p0-report.md` 历史结论。
- 工作树中已有的用户修改不得丢失；提交前执行 `git diff --check` 和敏感文件检查。

## 测试与阶段门

每次交付至少执行：

- `hvigor assembleHap`；
- `scripts/NoWebOutsideLogin.ps1`；
- `scripts/CheckBottomTabVisualLock.ps1`；
- 模拟器 signed HAP 覆盖安装、启动、`dumpLayout` 与进程日志检查；
- 涉及 SDK、材质或共享玻璃时，至少分别复验 Pura 90 API 24 与 API 26 模拟器；里程碑最后再做 API 26 真机复验，不得把模拟器或离线真机记为真机 PASS。

当前仓库按维护者明确要求不保留 `entry/src/test`、`entry/src/ohosTest` 与 `entry/src/mock`。删除前的 91/91 单元测试和 13/13 模拟器设备测试只能作为历史证据，后续代码变化不得借用该结果宣称自动回归 PASS；如重新引入自动测试，必须放回标准目录并同步 `docs/testing.md`。

安全日志硬性检查：Cookie 值泄露 0、`UI_FALLBACK` 0、Fatal 0、自动化真实 POST 0。

## 当前状态（2026-08-30）

- M3 四栏原生社区壳、首页独立发布 FAB、蓝色语义主题、首页真实分页、板块筛选、搜索、个人页、发帖编辑器和原生回复栏：`PASS`（实现与模拟器验收）。
- 主题详情原生排版、头像左/内容右、楼中楼层级、网页“展开全文”控件隔离和图片可视区懒加载：`PASS`。
- Cloudflare 恢复链路：`PASS`（Pura 90 API 24 模拟器）。直连 RCP 复现 `HTTP 403` 与 `Cf-Mitigated: challenge` 后，同源 ArkWeb 传输宿主会先静默恢复；约 12 秒仍未恢复才显示官方挑战页。挑战完成后原生首页自动重载，进程重启后会话仍可复用。
- 本轮 HDS 根导航/页签、264×56vp 四栏玻璃主栏、单一连续液态指示器和首页右侧同高 56vp 发帖按钮：实现已更新，设备状态以本轮验收记录为准。板块/称号不显示右侧动作并用弹簧动画居中，“我的”右侧改为系统设置按钮。浅色模式保留底层头像与文字色块，深色模式使用低 alpha 冷色采样和细亮边；所有玻璃组件只复用 4vp 通透配方，不再存在奶白选项。
- 独立设置页与偏好切换：设置不占用个人主页，由“我的”底栏右侧系统齿轮进入。总览以圆角分组显示外观主题和首页帖子样式的当前值；主题详情使用浅色/深色两列大预览与下方独立“跟随系统”开关，帖子样式详情使用两张排版预览。玻璃风格入口已删除，升级时旧奶白值自动迁移为通透。
- 邀请测试候选版本：`1.1.1 / 1001001 / build 3`。该版本包含 Cloudflare 同源恢复、头像加载和滑动/切页热路径修复；release 签名 `.app` / `.hap` 已在本地组装并通过签名、Profile 和包内版本校验。设置总览的四档一次性 IAP 弹窗与 `topic/17803` 原生跳转保持不变。真实华为收银台交易、AGC 商品配置、App Pack 上传/解析和邀请测试审核仍为 `NOT RUN`，不得由本地弹窗、签名构建或 HAP 安装推断通过。
- 发帖登录门禁与官方登录安全区：`PASS`（Pura 90 API 24 设备回归）；未登录门禁固定在标题栏正文区域起点 12vp 内且无容器框，设备断言上限为 16vp；官方登录原生头部按系统顶部 inset 下移并删除旧说明句，标题和完成按钮均通过安全区位置断言。
- 搜索/回复操作与页面层级：未登录搜索输入/提交/范围控件为 0，只显示与“我的”同尺寸、同位置门禁；登录后搜索文字取消默认内缩，42vp 输入与提交按钮上下边界一致，搜索与回复共用 24vp/800 `arrow_up`，搜索仅旋转 90°；42vp 回复输入与发送按钮上下边界一致，发送端无蓝色圆底且箭头始终使用品牌蓝。四个根页标题为 28vp/Bold，压栈标题为 22vp/Bold，首页、板块目录与板块主题为 17vp/500；本轮设备状态以最新验收记录为准。
- 原生性审计：常规业务页面、导航、图标、解码与编辑器均为 HarmonyOS 原生实现；正常布局树固定有 1 个被原生根壳完全遮挡的同源传输 Web，唯一可见网页内容是用户主动打开的 linux.sb 官方登录页或 Cloudflare 挑战页。BBS1 HTML 仍只作为数据协议解码，不作为网页 UI 渲染。
- 普通组件 `.systemMaterial(new uiMaterial.ImmersiveMaterial(...))`：当前 target API 24 **未接入/未运行**；官方起始版本为 26.0.0，必须等 target 26 工具链恢复并留下构建与设备证据后再更新状态，不能从 HDS 能力探测推断。
- 自动测试历史：删除测试源码前的单元测试 91/91 与模拟器设备测试 13/13 为 `PASS`；当前活动自动测试套件为 `NOT RUN`（源码已按维护者要求移除）。历史覆盖包括四栏居中、单一原生底栏图标、拖动指示器、FAB、设置即时勾选、板块、编辑器、登录关闭、原生详情、42vp 搜索/回复控件和标题切换器居中。
- `NoWebOutsideLogin`、底栏视觉锁、signed HAP 构建/覆盖安装/启动：`PASS`。本轮首页恢复后的 `dumpLayout` 为隐藏传输 Web 1、首主题 1、主题项 14、挑战标题 0、错误节点 0；不得再沿用旧的普通页面 Web 0 断言。
- Pura 90 模拟器与 Mate 80 Pro Max 真机最终应用进程：Fatal 0、`UI_FALLBACK` 0、Cookie 标识符日志 0、HTTP POST 0。
- Pura 90 API 24 在头像加载期间完成 4 轮双向惯性滑动，并快速切页 12 次；Mate 80 Pro Max API 26 也快速切页 12 次，两台设备的 RenderService `>16.67 / 33 / 66ms` 三档卡顿帧均为 0。真机 HiSmartPerf 首页单次滑动为 `90.6017fps / 36ms / 0.00ms/s / 最大连续丢帧 0`；AppGallery 自动化 90.9ms/s 指标仍需用新包重测。
- 性能样本：点击到目的页约 9ms，Skeleton 约 26ms，含图主题首段文字约 968ms，TaskPool 解码约 14ms；首图受外部 CDN 影响约 5.06s，现已改为不阻塞正文的可视区懒加载。
- 真实社区 POST：`NOT RUN`；测试期间未发帖、未回复。
- API 26 真机本轮首页头像、滑动和根页面切换专项复验：`PASS`（Mate 80 Pro Max / SGT-AL10 / 本地调试签名 HAP）；这不等同于邀请测试 release 包、AppGallery 自动化任务、支付或整个 M3 发布验收通过。

## 文档规则

- 根目录只保留文档入口 `README.md` 与实现约束 `AGENTS.md`；其他 Markdown 一律放入 `docs/`，禁止把阶段报告、截图说明或临时笔记重新堆回根目录。
- `docs/index.md` 是文档索引；架构、UI、开发、测试和安全规范分别维护在 `docs/architecture.md`、`docs/ui.md`、`docs/development.md`、`docs/testing.md` 与 `docs/security.md`。
- UI 变化必须同步更新 `docs/ui.md`；技术路线或原生边界变化必须同步更新 `docs/architecture.md` 与 `docs/security.md`；阶段状态变化更新 `docs/project-status.md`。`README.md` 只保留面向读者的准确摘要。
- `docs/p0-report.md` 保持历史冻结，不随当前 UI 迭代改写。
- 所有验收结果严格使用 `PASS`、`FAIL` 或 `NOT RUN`，未执行真机或真实写入时不得补写 PASS。
