# Codex Instructions — 烧饼社区 M3 原生社区壳与交互闭环

## 当前唯一任务

在不推翻 P0、P0-B 和 M1 已验证架构的前提下，完成简约、蓝色、纯 ArkUI 的社区主流程：

```text
底部：首页 / 板块 / 搜索 / 我的；首页独立发布 FAB
  ↓
真实列表、真实头像、连续分页
  ↓
原生主题详情、楼中楼、图片和简约回复栏
```

当前分支为 `codex/m3-native-community-shell`。下方 M2 的阶段结论已转入 `PROJECT_BASELINE.md` 作为历史；如与本文冲突，以本 M3 指令为准。

## 必须保持的架构

- 正常业务：`RCP -> 版本化轻量解码 -> TaskPool -> 领域模型 -> PresentationMapper -> ArkUI`。
- Page / Component 不直接解析 HTML、读 Cookie 或绕过 Repository 请求网络。
- 不构建 DOM，不使用 CSS 选择器，不使用整页正则解码。
- ArkWeb 只允许出现在 `OfficialLoginPage`，只用于 linux.sb 官方登录；其他页面 Web 节点必须为 0。
- Cookie 只保留 `bbs_auth` / `bbs_csrf` 所需的内存 Header，不输出值，不写 Preferences 或文件。
- 全局复用正式 `RcpForumTransport` / Session，主题页保留缓存、在途去重和立即导航。

## UI 基线

- 根导航和页签使用 `HdsNavigation / HdsNavDestination / HdsTabs`；可见底栏只承载四个居中 Tab：`首页 / 板块 / 搜索 / 我的`，发布入口是首页右下方可拖动 FAB，不得再把加号塞进 Tab。底栏固定为 56vp“鸿蒙高亮”玻璃，不再提供紧凑或 iOS 风格切换；全程只允许一个玻璃托盘和一个 64×50vp 连续移动的选中指示器，指示器按 72vp Tab 槽位精确居中，按钮自身禁用默认 pressed/hover 状态层，避免黑闪和双层轮廓。
- 四个底栏图标只保留一套鸿蒙原生实现，不再提供自有 SVG 或图标风格切换。板块、搜索和我的分别使用系统填充 `folder_fill`、`doc_text_magnifyingglass_fill`、`person_fill` `SymbolGlyph`；首页因 API 24 的系统房屋字形屋顶分离，使用 ArkUI `Path` 按 24×24 参考坐标绘制，并通过 `UIContext.vp2px()` 适配设备密度。首页门洞保留透明，底边以 1.85 路径单位的加粗横梁封闭。四枚图标放入相同的 28×26vp 光学盒，在保留各自轮廓、比例和光学补偿的基础上，从各自中心统一乘以 0.92 的最终显示缩放，再整体统一下移 0.7vp 以收紧图标与标签间距；四个标签统一使用 11vp 字号、16vp 行盒并共享同一基线。`ImmersiveBottomTabBar.ets` 的当前视觉实现已冻结，未经用户明确要求不得修改；交付前必须通过 `scripts/CheckBottomTabVisualLock.ps1` 的 SHA-256 校验。首页 FAB 的加号固定为 23vp 系统 `plus`、900 字重；拖动只允许在显示帧边界提交一次合并位移，不得在每个原始手势事件中同时改位置和玻璃缩放，也不得使用大于 1vp 的起步阈值造成首帧跳动。
- HDS 材质默认 `ADAPTIVE`，启动时以 `getSystemMaterialTypes()` 查询设备能力；不支持时退化为 `NONE`，不得在低能力设备强制 `EXQUISITE`。
- 语义主色为蓝色；禁止绿色品牌色泄漏。
- 页面背景保持单一底色；仅输入、分段切换和悬浮导航等交互层允许使用半透明液态玻璃，不得再用不透明 surface 切割页面。
- 标题栏继续使用 HDS `GRADIENT_BLUR`；所有自定义玻璃控件统一使用一个 `backgroundEffect` 采样层，采样半径为 6vp，默认饱和度 1.08、亮度 1.12，并使用约 1.25vp 白色高亮边缘与单圈中性外阴影。同一节点不得再叠加不透明/半透明背景或第二个系统模糊层；浅色模式的选中胶囊使用较淡的中性灰透明填充，下方内容保留轮廓但不得与控件内文字抢读。
- 首页、具体板块、搜索和个人页的模式切换统一使用 `CompactSegmentedControl`；浅色模式的活动指示器必须使用可辨认的中性灰胶囊，不得与页面背景融为一体。首页标题使用 22vp，标题栏“新评论 / 新帖子”切换使用 66vp 单项宽、36vp 高的放大规格。
- 板块根页只显示真实板块目录，并使用 16vp 不同色标、板块名、官方简介、真实主题数和进入箭头；板块名称和非首页主题标题保持 16vp、400 字重，主题元信息保持 13vp。首页仅向用户提供并持久化两种展示：默认“标题优先”为标题、标签、作者/回复/时间三行，头像 48vp；“作者优先”为头像旁作者与真实最近活跃时间/回复数、下方标题、末行标签，头像缩至 38vp。两种首页标题均为 17vp、500 字重，不得重新暴露旧样式一；非首页主题流继续使用原紧凑结构。进入具体板块后才显示主题流及“新评论 / 新帖子”切换。帖子状态统一使用浅色语义胶囊：置顶蓝、精华紫、热红、抽奖中粉、进行中卡片青、已结束卡片灰，并为深色模式提供对应高对比色。
- 搜索提交、官方登录和主题回复发送等高频操作不得使用纯色填充按钮；统一复用首页 FAB 的 6vp 单层玻璃、细高光边框和轻光晕，图标或文字使用品牌蓝。主题回复发送按钮与输入框外层均为 44vp 高；按钮内继续使用 36vp、700 字重的系统 `arrow_up_circle_fill`，不得改回纯色圆或细箭头。
- 主页不永久显示“非官方客户端”；身份说明保留在登录/关于等合适位置。
- 列表使用 linux.sb 真实头像；只在缺失或加载失败时显示首字母回退。
- 主楼、回复和楼中楼均为头像居左、作者/正文居右；使用克制的局部分隔，不还原网页容器和强分割线。
- 图片使用原生 `Image` 可视区懒加载，不阻塞文字首帧；失败时可重试且不让页面停留 Loading。
- 主题详情与发表主题页共用同一返回按钮：40vp 单层玻璃点击区，`chevron_left` 使用 28vp、700 字重，并保持相同标题栏内边距。回复输入不保留左侧编辑图标，只使用输入文字和单一发送按钮。帖子内“回复”动作必须使用胶囊圆角并裁剪点击状态，禁止出现矩形闪层。
- 发表主题页未登录时不得渲染发布按钮、板块选择、标题或正文输入，只显示登录门禁；门禁使用顶部锚定的独立布局，固定在“发表主题”标题栏正下方，取消边框、背景框和大块垂直留白。登录后才显示完整编辑器。真实板块必须全部以两列网格直接展示，发布操作使用 40vp 圆润单层玻璃胶囊。
- 官方登录覆盖页的原生头部必须读取 `SYS_TOP_INSET_KEY`，标题与“完成并返回”按钮完整避开状态栏和挖孔区；不显示“临时网页登录……”说明。登录正文仍是唯一允许的临时 ArkWeb，普通页面不得出现 Web 节点。
- 用户已确认获得 ArkDO 作者授权；可在授权范围内参考、移植和改造固定上游提交 `7680996437b3b877aa5c69ac2f55529297a2ea52`，但保留烧饼社区自己的品牌、协议模型和网络架构。

## 数据与交互契约

- 首页根据服务端真实 `p` 链接连续加载，不得再有 30 条客户端上限；合并时按 topicId 去重。
- 板块页只展示服务端实际板块，选中后通过真实 `/forum/{id}` GET 筛选。
- 搜索使用真实 GET 契约，尊重服务端登录门控，不伪造结果。
- “我的”使用原生页面展示当前用户、主题和回复，不使用 ArkWeb 作业务容器；外观设置只放在独立原生设置页，由“我的”页可拖动玻璃齿轮 FAB 进入。
- 新主题只允许 POST `/topic_edit`，回复只允许 POST `/reply_edit`；字段必须匹配 BBS1 v8.6.5 真实表单契约。
- 写请求必须同时拥有内存登录会话和 CSRF，且只能由用户在编辑页明确点击。自动测试只使用 fake transport，不得生成真实社区内容。
- P0 报告中 `P0-8` 的历史结论继续为 `NOT RUN`；M3 的真实发帖/回复远程验收也保持 `NOT RUN`，直到用户明确提供测试内容并确认发送。

## 安全与 Git

- 可共享的 `build-profile.json5` 不得包含本机签名密码、证书路径、keystore 路径或 Profile 路径；本地调试签名由 DevEco Studio 在开发机上单独配置，不能提交到 Git。
- 不提交 `artifacts/`、日志、截图、HTML 快照、Cookie 或签名材料。
- 不修改 `P0_REPORT.md`历史结论。
- 工作树中已有的用户修改不得丢失；提交前执行 `git diff --check` 和敏感文件检查。

## 测试与阶段门

每次交付至少执行：

- `hvigor test`；
- `hvigor onDeviceTest`；
- `hvigor assembleHap`；
- `scripts/NoWebOutsideLogin.ps1`；
- `scripts/CheckBottomTabVisualLock.ps1`；
- 模拟器 signed HAP 覆盖安装、启动、`dumpLayout` 与进程日志检查；
- 里程碑最后再做 API 26 真机复验，不得把离线真机记为 PASS。

安全日志硬性检查：Cookie 值泄露 0、`UI_FALLBACK` 0、Fatal 0、自动化真实 POST 0。

## 当前状态（2026-08-26）

- M3 四栏原生社区壳、首页独立发布 FAB、蓝色语义主题、首页真实分页、板块筛选、搜索、个人页、发帖编辑器和原生回复栏：`PASS`（实现与模拟器验收）。
- 主题详情原生排版、头像左/内容右、楼中楼层级、网页“展开全文”控件隔离和图片可视区懒加载：`PASS`。
- 临时登录关闭顺序：`PASS`；先移除 ArkWeb，下一帧再抓取内存 Cookie 并刷新。手工复验 600ms 后 Profile 节点 1、Web 节点 0；设备自动回归同样通过。
- 本轮 HDS 根导航/页签、居中四栏玻璃主栏、单一连续液态指示器、可拖动首页 FAB、16vp 彩色板块色标和带简介/主题数的板块目录：`PASS`（Pura 90 API 24 可视复验）。浅色模式的底栏与分段控件活动指示器已改为可辨认的中性灰；全局玻璃已压低染色并删除输入框/回复栏的重复背景层；未登录 Search/Profile 只保留登录标题和动作。
- 独立设置页与偏好切换：`PASS`；“我的”页通过可拖动玻璃齿轮 FAB 进入设置，设置不再占用个人主页；持久化“跟随系统 / 浅色 / 深色”和首页“标题优先 / 作者优先”。错误的双套底栏图标偏好、SVG 资源和切换控件已移除。设置内容固定在标题栏下方，不再因短内容垂直居中。
- 发帖登录门禁与官方登录安全区：`PASS`（Pura 90 API 24 设备回归）；未登录门禁固定在标题栏正文区域起点 12vp 内且无容器框，设备断言上限为 16vp；官方登录原生头部按系统顶部 inset 下移并删除旧说明句，标题和完成按钮均通过安全区位置断言。
- 原生性审计：常规业务页面、导航、图标、网络、解码与编辑器均为 HarmonyOS 原生实现；唯一非 ArkUI 可见内容是用户主动打开的 linux.sb 官方登录网页。远程头像/正文图片由原生 `Image` 承载；BBS1 HTML 是数据协议，不作为网页 UI 渲染。
- 普通组件 `.systemMaterial(new uiMaterial.ImmersiveMaterial(...))`：`NOT RUN`；当前本机 SDK 与工程 compile/target API 为 24，而该 ArkUI API 从 API 26 提供。不得伪造类型；升级 API 26 SDK 后再接入。
- 单元测试：`PASS`，91/91；模拟器设备测试：`PASS`，11/11（覆盖四栏居中、四枚单一原生图标与旧矢量节点为 0、单一鸿蒙高亮底栏、连续拖动指示器、可拖动 FAB、三档主题、板块进入、原生编辑器、临时登录关闭和原生主题详情）。
- `NoWebOutsideLogin`、本轮 Home/板块目录/具体板块/Search/Profile/Topic 六类 `dumpLayout` Web 0、signed HAP 构建/覆盖安装/启动：`PASS`。
- 模拟器最终应用进程：Fatal 0、`UI_FALLBACK` 0、Cookie 泄露 0、HTTP POST 0。
- 性能样本：点击到目的页约 9ms，Skeleton 约 26ms，含图主题首段文字约 968ms，TaskPool 解码约 14ms；首图受外部 CDN 影响约 5.06s，现已改为不阻塞正文的可视区懒加载。
- 真实社区 POST：`NOT RUN`；测试期间未发帖、未回复。
- API 26 真机本轮最终复验：`NOT RUN`；HDC 重启后仅发现 Pura 90 模拟器，Windows 未枚举到 HDC Interface，不得宣称 M3 真机 PASS。

## 文档规则

- 当前文档只维护 `README.md`、`AGENTS.md`、`PROJECT_BASELINE.md` 和已冻结的 `P0_REPORT.md`，不新建成套阶段文档。
- UI、技术路线或原生边界变化必须同步更新 `README.md`、`AGENTS.md` 和 `PROJECT_BASELINE.md`；`P0_REPORT.md` 保持历史冻结，不随当前 UI 迭代改写。
- 所有验收结果严格使用 `PASS`、`FAIL` 或 `NOT RUN`，未执行真机或真实写入时不得补写 PASS。
